import alt from '../lib/alt'

class BeerActions {
  // TODO: create methods to pass through info from views and to the store. see example below from todos list.


  // setList(list) {
  //   //return is the same thing as dispatch
  //   return list;
  // }
}

export default alt.createActions(BeerActions)
